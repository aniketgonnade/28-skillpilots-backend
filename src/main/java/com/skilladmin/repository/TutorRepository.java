package com.skilladmin.repository;

import com.skilladmin.model.Tutors;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;

public interface








































































































































TutorRepository extends JpaRepository<Tutors,Long> {

    public  Tutors findByEmail(String email);
    public List<Tutors> findByStatus(boolean status);

}
